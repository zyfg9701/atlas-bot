{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "Default capability for atlas-bot PC (I2: Tauri rust WS with Bearer)",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
