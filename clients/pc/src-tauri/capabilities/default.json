{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "Default capability for atlas-bot PC (browser WebSocket only)",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
