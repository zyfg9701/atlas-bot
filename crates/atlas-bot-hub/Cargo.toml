[package]
name = "atlas-bot-hub"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Private Computer Hub service shell (stub — no business until license audit)"

[dependencies]
xai-tool-protocol = { path = "../../third_party/xai-tool-protocol" }
