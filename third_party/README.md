# third_party

Vendored Bot-Relay protocol crates only. Do **not** vendor entire `grok-build`.

- `xai-tool-protocol` — Bot-Relay types, fixtures, conformance tests, generated Kotlin/Swift
- `xai-tool-types` — direct dependency of the protocol crate

Pinned via `docs/SOURCE_REV.md`.
